apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "{{ storageclass_name }}"
provisioner: "{{ storageclass_provisioner }}"
reclaimPolicy: "{{ storageclass_reclaimpolicy }}"
parameters:
  {{ storageclass_parameters | to_nice_yaml(indent=2) }}
